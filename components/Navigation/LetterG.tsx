type LetterGProps = {
  className?: string;
};

/** Figma component: G (node 1434:2). */
export default function LetterG({ className }: LetterGProps) {
  return (
    <div
      className={`relative flex h-full w-full items-center justify-center overflow-visible ${className ?? ""}`}
      data-node-id="1434:2"
      data-name="G"
    >
      <span className="block font-[family-name:var(--font-zilla)] text-[clamp(1.25rem,5vw,3rem)] font-normal leading-[0.9] text-white">
        G
      </span>
    </div>
  );
}
